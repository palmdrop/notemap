<!--
  Starts where the content column starts and runs past the register's padding to
  meet the spine, so every capture makes one junction against it.
-->
<hr
  class="my-0 -mr-spine ml-[calc(var(--spacing-gutter)+var(--spacing-gap))] border-t border-ink max-narrow:ml-0"
/>
