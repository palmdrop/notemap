{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "notemap",
  "title": "Notemap",
  "description": "Capture a quick note",
  "icon": "extension-icon.png",
  "author": "palmdrop",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Productivity",
    "Applications",
    "Web"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "capture",
      "title": "Capture",
      "subtitle": "Notemap",
      "description": "Capture a note into your Notemap pool, or into the outbox until it can be reached",
      "mode": "view"
    },
    {
      "name": "drain",
      "title": "Drain Outbox",
      "subtitle": "Notemap",
      "description": "Send what was captured while the pool was unreachable",
      "mode": "no-view",
      "interval": "10m"
    }
  ],
  "dependencies": {
    "@notemap/client": "workspace:*",
    "@raycast/api": "^2.4.1",
    "@raycast/utils": "^2.3.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "^24.10.1",
    "@types/react": "19.0.10",
    "eslint": "^10.8.0",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "preferences": [
    {
      "name": "daemonUrl",
      "title": "Daemon URL",
      "description": "Where the Notemap daemon answers",
      "type": "textfield",
      "required": true,
      "default": "http://127.0.0.1:4747"
    },
    {
      "name": "token",
      "title": "Access token",
      "description": "A token the daemon issued, for a daemon that asks for one",
      "type": "password",
      "required": false
    }
  ]
}
