# The version to run. Pin a release rather than tracking `latest`, so the
# upgrade is a line changed here and a rollback is the line changed back.
NOTEMAP_VERSION=latest

# Only read by `compose.proxy.yaml`: an existing Docker network the reverse
# proxy is on. Compose does not create it.
PROXY_NETWORK=proxy
