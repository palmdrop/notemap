# The version to run. Pin a release rather than tracking `latest`, so the
# upgrade is a line you change here and a rollback is the line you change back.
NOTEMAP_VERSION=latest

# Only read by `compose.proxy.yaml`: the existing Docker network your reverse
# proxy is on. Compose does not create it.
PROXY_NETWORK=proxy
